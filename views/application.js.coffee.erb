$ ->
  $('#query').keyup(() ->
    if $(this).val().length >= 0
      query = { q: $(this).val(), type: $(this).data('search-type') }
      result = $.get('<%= url_for "/search_tweet" %>', query, (data) ->
        eval(data)
      , 'text')
  )
